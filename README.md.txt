The project is done with Django Rest Frame Work Api and ReactJs Front End UI with Bootstrap.

Follow the instruction to execute the application:
​
1) run Python application:
python manage.py runserver

2) download the packages for ReactJs application:
npm install

3) run reactjs application:
npm start
